# Dependencies for SST_Dashboard scripts and GUIs.
# Install from repo root with:
#   python -m pip install -r SST_Dashboard/requirements.txt

# Core numerics / data
numpy>=1.19.0
pandas>=2.0.0
scipy>=1.10.0
matplotlib>=3.8.0
requests>=2.31.0

# GUI stack
PyQt5>=5.15.0
PyQtWebEngine>=5.15.0

# Optional acceleration / analysis backends used by some dashboard flows
# (safe to omit if you only use numpy/local_cpp_scan backends)
torch>=2.0.0

# Optional knot-analysis helper used by parse_knots.py
pyknotid>=0.5.3
