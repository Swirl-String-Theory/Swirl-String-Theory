# Deprecated — use Angular project

This React + CDN importmap prototype has been replaced by a standard Angular CLI app:

**`../angular`**

```bash
cd ../angular
npm install
npm start
```

The Angular version includes the same sections, CMS admin panel, Three.js scenes, KaTeX math, knot tables, and Starship Coil simulator.
