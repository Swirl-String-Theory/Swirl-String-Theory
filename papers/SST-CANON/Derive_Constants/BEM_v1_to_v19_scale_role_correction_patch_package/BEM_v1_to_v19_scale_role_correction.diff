diff -ruN a/BEM_SCALE_ROLE_CONVENTION.md b/BEM_SCALE_ROLE_CONVENTION.md
--- a/BEM_SCALE_ROLE_CONVENTION.md	1970-01-01 00:00:00.000000000 +0000
+++ b/BEM_SCALE_ROLE_CONVENTION.md	2026-06-23 10:18:47.256423000 +0000
@@ -0,0 +1,53 @@
+# BEM scale-role convention
+
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
diff -ruN a/BEMv14_extensive_multigrid_suite/README_BEMv14_extensive_multigrid_suite.md b/BEMv14_extensive_multigrid_suite/README_BEMv14_extensive_multigrid_suite.md
--- a/BEMv14_extensive_multigrid_suite/README_BEMv14_extensive_multigrid_suite.md	2026-06-23 10:18:03.010574609 +0000
+++ b/BEMv14_extensive_multigrid_suite/README_BEMv14_extensive_multigrid_suite.md	2026-06-23 10:18:47.259759841 +0000
@@ -1,4 +1,56 @@
 # BEMv14 extensive multigrid suite
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 Dit pakket is de uitgebreide variant van de BEMv14 multirun-audit.
 
diff -ruN a/BEMv14_extensive_multigrid_suite/README_routeB_RT_bem_v14_multirun_grids.md b/BEMv14_extensive_multigrid_suite/README_routeB_RT_bem_v14_multirun_grids.md
--- a/BEMv14_extensive_multigrid_suite/README_routeB_RT_bem_v14_multirun_grids.md	2026-06-23 10:18:03.299968780 +0000
+++ b/BEMv14_extensive_multigrid_suite/README_routeB_RT_bem_v14_multirun_grids.md	2026-06-23 10:18:47.260818967 +0000
@@ -1,4 +1,56 @@
 # BEMv14 multirun over multiple grids
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 Ja — dit is inderdaad nodig als je meer wilt dan één intra-grid check.
 
diff -ruN a/BEMv18_extended_local_scan/README_BEMv18_extended_local_scan.md b/BEMv18_extended_local_scan/README_BEMv18_extended_local_scan.md
--- a/BEMv18_extended_local_scan/README_BEMv18_extended_local_scan.md	2026-06-23 10:17:57.387978250 +0000
+++ b/BEMv18_extended_local_scan/README_BEMv18_extended_local_scan.md	2026-06-23 10:18:47.261996555 +0000
@@ -1,4 +1,56 @@
 # BEMv18 extended local scan — up to 7 crossings plus selected higher knots
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 Deze variant is gemaakt voor jouw exacte request:
 
diff -ruN a/BEMv18_extended_local_scan/routeB_RT_bem_v18_extended_local_scan.py b/BEMv18_extended_local_scan/routeB_RT_bem_v18_extended_local_scan.py
--- a/BEMv18_extended_local_scan/routeB_RT_bem_v18_extended_local_scan.py	2026-06-23 10:17:57.476125303 +0000
+++ b/BEMv18_extended_local_scan/routeB_RT_bem_v18_extended_local_scan.py	2026-06-23 10:18:47.263519889 +0000
@@ -23,6 +23,36 @@
 then calls BEMv18 in reuse mode to aggregate the exponent scan.
 
 No observed alpha is used.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/BEMv18_extended_local_scan_knotplot/README_BEMv18_extended_local_scan_knotplot.md b/BEMv18_extended_local_scan_knotplot/README_BEMv18_extended_local_scan_knotplot.md
--- a/BEMv18_extended_local_scan_knotplot/README_BEMv18_extended_local_scan_knotplot.md	2026-06-23 10:17:56.680056862 +0000
+++ b/BEMv18_extended_local_scan_knotplot/README_BEMv18_extended_local_scan_knotplot.md	2026-06-23 10:18:47.264435947 +0000
@@ -1,4 +1,56 @@
 # BEMv18 KnotPlot-aware extended scan
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 Deze variant ondersteunt nu ook jouw nieuw toegevoegde KnotPlot-notatie:
 
diff -ruN a/BEMv18_extended_local_scan_knotplot/routeB_RT_bem_v18_extended_local_scan_knotplot.py b/BEMv18_extended_local_scan_knotplot/routeB_RT_bem_v18_extended_local_scan_knotplot.py
--- a/BEMv18_extended_local_scan_knotplot/routeB_RT_bem_v18_extended_local_scan_knotplot.py	2026-06-23 10:17:57.214656232 +0000
+++ b/BEMv18_extended_local_scan_knotplot/routeB_RT_bem_v18_extended_local_scan_knotplot.py	2026-06-23 10:18:47.265484835 +0000
@@ -24,6 +24,36 @@
 unless you explicitly override the scripts downstream.
 
 No observed alpha is used.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/BEMv19_link_geometry_parser/README_BEMv19_link_geometry_parser.md b/BEMv19_link_geometry_parser/README_BEMv19_link_geometry_parser.md
--- a/BEMv19_link_geometry_parser/README_BEMv19_link_geometry_parser.md	2026-06-23 10:17:56.410479001 +0000
+++ b/BEMv19_link_geometry_parser/README_BEMv19_link_geometry_parser.md	2026-06-23 10:18:47.267593777 +0000
@@ -1,4 +1,56 @@
 # BEMv19 link geometry parser
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv19 voegt de aparte multi-component parser toe voor links.
 
diff -ruN a/BEMv19_link_geometry_parser/routeB_RT_bem_v19_link_geometry_parser.py b/BEMv19_link_geometry_parser/routeB_RT_bem_v19_link_geometry_parser.py
--- a/BEMv19_link_geometry_parser/routeB_RT_bem_v19_link_geometry_parser.py	2026-06-23 10:17:56.259768700 +0000
+++ b/BEMv19_link_geometry_parser/routeB_RT_bem_v19_link_geometry_parser.py	2026-06-23 10:18:47.268597686 +0000
@@ -59,6 +59,36 @@
   link_routeB_integration_plan.md
   link_multicomponent_appendix.tex
   run_config_v19.json
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/README_BEM_SCALE_ROLE_PATCH.md b/README_BEM_SCALE_ROLE_PATCH.md
--- a/README_BEM_SCALE_ROLE_PATCH.md	1970-01-01 00:00:00.000000000 +0000
+++ b/README_BEM_SCALE_ROLE_PATCH.md	2026-06-23 10:18:47.257458121 +0000
@@ -0,0 +1,28 @@
+# Cursor patch: BEM scale-role correction
+
+This patch updates the BEM source comments and README text so that the scale
+roles are stated consistently across the Route-B BEM series.
+
+Core correction:
+
+```text
+r_c is the horn-torus / return-flow circulation radius R_horn.
+r_c is not, by default, the local ideal-tube radius a_tube.
+```
+
+Dimensionless BEM formulae remain unchanged:
+
+```text
+N_RT = M_max * L_cert^2
+```
+
+Physical reconstruction must use:
+
+```text
+a_tube = r_c / chi_h
+ell_K_phys = 2 * a_tube * L_cert
+```
+
+This patch is intentionally conservative: it does not change numerical BEM
+kernels or historical output values. It only prevents the wrong physical
+interpretation from propagating into BEMv1--BEMv19 documentation and docstrings.
diff -ruN a/README_routeB_RT_bem_stecklov_falsifier.md b/README_routeB_RT_bem_stecklov_falsifier.md
--- a/README_routeB_RT_bem_stecklov_falsifier.md	2026-06-23 10:18:15.209622019 +0000
+++ b/README_routeB_RT_bem_stecklov_falsifier.md	2026-06-23 10:18:47.269467270 +0000
@@ -1,4 +1,56 @@
 # Route B v2: R--T BEM/Steklov falsifier
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 This replaces the earlier 1D curve surrogate with a 3D boundary-integral candidate on a knot-tube complement.
 
diff -ruN a/README_routeB_RT_bem_v10_normalizer_convergence.md b/README_routeB_RT_bem_v10_normalizer_convergence.md
--- a/README_routeB_RT_bem_v10_normalizer_convergence.md	2026-06-23 10:18:06.742600923 +0000
+++ b/README_routeB_RT_bem_v10_normalizer_convergence.md	2026-06-23 10:18:47.270295530 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv10 normalizer convergence audit
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv10 checks whether a BEMv9 finite-correction normalizer remains stable over a refinement grid.
 
diff -ruN a/README_routeB_RT_bem_v11_length_provenance.md b/README_routeB_RT_bem_v11_length_provenance.md
--- a/README_routeB_RT_bem_v11_length_provenance.md	2026-06-23 10:18:06.351958964 +0000
+++ b/README_routeB_RT_bem_v11_length_provenance.md	2026-06-23 10:18:47.271142683 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv11 length provenance
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv11 audits the length bottleneck isolated by BEMv10.
 
diff -ruN a/README_routeB_RT_bem_v12_fourier_convention_audit.md b/README_routeB_RT_bem_v12_fourier_convention_audit.md
--- a/README_routeB_RT_bem_v12_fourier_convention_audit.md	2026-06-23 10:18:05.582822444 +0000
+++ b/README_routeB_RT_bem_v12_fourier_convention_audit.md	2026-06-23 10:18:47.271905991 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv12 Fourier-convention audit
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv12 investigates the BEMv11 length provenance mismatch:
 
diff -ruN a/README_routeB_RT_bem_v13_certified_length_budget.md b/README_routeB_RT_bem_v13_certified_length_budget.md
--- a/README_routeB_RT_bem_v13_certified_length_budget.md	2026-06-23 10:18:05.106252159 +0000
+++ b/README_routeB_RT_bem_v13_certified_length_budget.md	2026-06-23 10:18:47.272856705 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv13 certified-length budget
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv13 implements the certified-length branch suggested by BEMv12.
 
diff -ruN a/README_routeB_RT_bem_v14_certified_convergence.md b/README_routeB_RT_bem_v14_certified_convergence.md
--- a/README_routeB_RT_bem_v14_certified_convergence.md	2026-06-23 10:18:04.330591517 +0000
+++ b/README_routeB_RT_bem_v14_certified_convergence.md	2026-06-23 10:18:47.273894540 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv14 certified-length convergence
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv14 is the convergence-grid version of BEMv13.
 
diff -ruN a/README_routeB_RT_bem_v14_multirun_grids.md b/README_routeB_RT_bem_v14_multirun_grids.md
--- a/README_routeB_RT_bem_v14_multirun_grids.md	2026-06-23 10:18:03.699668838 +0000
+++ b/README_routeB_RT_bem_v14_multirun_grids.md	2026-06-23 10:18:47.275004188 +0000
@@ -1,4 +1,56 @@
 # BEMv14 multirun over multiple grids
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 Ja — dit is inderdaad nodig als je meer wilt dan één intra-grid check.
 
diff -ruN a/README_routeB_RT_bem_v15_normalizer_law.md b/README_routeB_RT_bem_v15_normalizer_law.md
--- a/README_routeB_RT_bem_v15_normalizer_law.md	2026-06-23 10:18:02.187639253 +0000
+++ b/README_routeB_RT_bem_v15_normalizer_law.md	2026-06-23 10:18:47.275822554 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv15 normalizer law
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv15 starts the analytic step after BEMv14.
 
diff -ruN a/README_routeB_RT_bem_v16_heat_kernel_normalizer.md b/README_routeB_RT_bem_v16_heat_kernel_normalizer.md
--- a/README_routeB_RT_bem_v16_heat_kernel_normalizer.md	2026-06-23 10:18:00.944665432 +0000
+++ b/README_routeB_RT_bem_v16_heat_kernel_normalizer.md	2026-06-23 10:18:47.276871190 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv16 heat-kernel normalizer
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv16 is the analytic/proof-obligation step after BEMv15.
 
diff -ruN a/README_routeB_RT_bem_v17_principal_symbol_heat_kernel.md b/README_routeB_RT_bem_v17_principal_symbol_heat_kernel.md
--- a/README_routeB_RT_bem_v17_principal_symbol_heat_kernel.md	2026-06-23 10:17:59.758313118 +0000
+++ b/README_routeB_RT_bem_v17_principal_symbol_heat_kernel.md	2026-06-23 10:18:47.278645037 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv17 principal-symbol / heat-kernel audit
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv17 sharpens the BEMv16 conditional certificate.
 
diff -ruN a/README_routeB_RT_bem_v18_multiknot_exponent_test.md b/README_routeB_RT_bem_v18_multiknot_exponent_test.md
--- a/README_routeB_RT_bem_v18_multiknot_exponent_test.md	2026-06-23 10:17:58.486118920 +0000
+++ b/README_routeB_RT_bem_v18_multiknot_exponent_test.md	2026-06-23 10:18:47.279382049 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv18 multi-knot exponent test
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv18 tests the BEMv17 claim that the residual length exponent is:
 
diff -ruN a/README_routeB_RT_bem_v3_1_sstcore_falsifier.md b/README_routeB_RT_bem_v3_1_sstcore_falsifier.md
--- a/README_routeB_RT_bem_v3_1_sstcore_falsifier.md	2026-06-23 10:18:14.609977756 +0000
+++ b/README_routeB_RT_bem_v3_1_sstcore_falsifier.md	2026-06-23 10:18:47.280360979 +0000
@@ -1,4 +1,56 @@
 # Route B BEM v3.1 with SSTcore ideal.txt loader
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 This is BEM v3 plus direct loading of ideal-knot Fourier data from the installed `SSTcore` package.
 
diff -ruN a/README_routeB_RT_bem_v3_2_idealxml_falsifier.md b/README_routeB_RT_bem_v3_2_idealxml_falsifier.md
--- a/README_routeB_RT_bem_v3_2_idealxml_falsifier.md	2026-06-23 10:18:13.749140683 +0000
+++ b/README_routeB_RT_bem_v3_2_idealxml_falsifier.md	2026-06-23 10:18:47.281274135 +0000
@@ -1,4 +1,56 @@
 # Route B BEM v3.2 — direct support for Brian Gilbert `ideal.txt`
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 This version works directly with the uploaded XML/Fourier ideal-knot database:
 
diff -ruN a/README_routeB_RT_bem_v3_3_idealxml_falsifier.md b/README_routeB_RT_bem_v3_3_idealxml_falsifier.md
--- a/README_routeB_RT_bem_v3_3_idealxml_falsifier.md	2026-06-23 10:18:12.756074089 +0000
+++ b/README_routeB_RT_bem_v3_3_idealxml_falsifier.md	2026-06-23 10:18:47.282107632 +0000
@@ -1,4 +1,56 @@
 # Route B BEM v3.3 — Brian Gilbert `ideal.txt` compatible
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 This is the Route-B BEM/Steklov falsifier updated for the **correct Brian Gilbert ideal-knot database** that includes `0:1:1` unknot control plus single-component knots and multi-component links.
 
diff -ruN a/README_routeB_RT_bem_v3_falsifier.md b/README_routeB_RT_bem_v3_falsifier.md
--- a/README_routeB_RT_bem_v3_falsifier.md	2026-06-23 10:18:14.636337794 +0000
+++ b/README_routeB_RT_bem_v3_falsifier.md	2026-06-23 10:18:47.283323780 +0000
@@ -1,4 +1,56 @@
 # Route B BEM v3 R--T spectral falsifier
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 This is the BEM v3 replacement for the previous Route-B surrogate.
 
diff -ruN a/README_routeB_RT_bem_v4_zeta_falsifier.md b/README_routeB_RT_bem_v4_zeta_falsifier.md
--- a/README_routeB_RT_bem_v4_zeta_falsifier.md	2026-06-23 10:18:12.281448196 +0000
+++ b/README_routeB_RT_bem_v4_zeta_falsifier.md	2026-06-23 10:18:47.284076173 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv4 zeta/cutoff falsifier
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv4 extends BEMv3.3 by saving raw spectra and building a cutoff-renormalized spectral-action audit.
 
diff -ruN a/README_routeB_RT_bem_v5_soft_hk_falsifier.md b/README_routeB_RT_bem_v5_soft_hk_falsifier.md
--- a/README_routeB_RT_bem_v5_soft_hk_falsifier.md	2026-06-23 10:18:11.574312532 +0000
+++ b/README_routeB_RT_bem_v5_soft_hk_falsifier.md	2026-06-23 10:18:47.285808851 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv5: soft-sector index + heat-kernel counterterms
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv5 extends BEMv4 by explicitly separating the candidate action into:
 
diff -ruN a/README_routeB_RT_bem_v6_convergence_grid.md b/README_routeB_RT_bem_v6_convergence_grid.md
--- a/README_routeB_RT_bem_v6_convergence_grid.md	2026-06-23 10:18:10.676787953 +0000
+++ b/README_routeB_RT_bem_v6_convergence_grid.md	2026-06-23 10:18:47.287008488 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv6 convergence-grid audit
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv6 wraps BEMv5 over a mesh/tube/outer-boundary grid.
 
diff -ruN a/README_routeB_RT_bem_v7_spectral_length_budget.md b/README_routeB_RT_bem_v7_spectral_length_budget.md
--- a/README_routeB_RT_bem_v7_spectral_length_budget.md	2026-06-23 10:18:10.391579123 +0000
+++ b/README_routeB_RT_bem_v7_spectral_length_budget.md	2026-06-23 10:18:47.288261860 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv7 spectral-length budget
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv7 implements the corrected component logic:
 
diff -ruN a/README_routeB_RT_bem_v8_pair_length_budget.md b/README_routeB_RT_bem_v8_pair_length_budget.md
--- a/README_routeB_RT_bem_v8_pair_length_budget.md	2026-06-23 10:18:08.157199693 +0000
+++ b/README_routeB_RT_bem_v8_pair_length_budget.md	2026-06-23 10:18:47.289808278 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv8 pair-subtracted length budget
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv8 fixes the two main BEMv7 pathologies:
 
diff -ruN a/README_routeB_RT_bem_v9_correction_normalizer.md b/README_routeB_RT_bem_v9_correction_normalizer.md
--- a/README_routeB_RT_bem_v9_correction_normalizer.md	2026-06-23 10:18:07.558872407 +0000
+++ b/README_routeB_RT_bem_v9_correction_normalizer.md	2026-06-23 10:18:47.291102510 +0000
@@ -1,4 +1,56 @@
 # Route B BEMv9 finite-correction normalizer audit
+## Scale-role convention: \(r_c\), \(R_{\rm horn}\), and \(a_{\rm tube}\)
+
+Route-B BEM is a **dimensionless certified-geometry programme**.  Its core
+normalizer uses the certified ropelength coordinate \(L_{\rm cert}\), retained
+mode count \(M_{\max}\), and pair correction \(\Delta F_{\rm pair}\).  It does
+not require inserting a physical core radius into the numerical BEM score.
+
+For physical interpretation, use the following scale separation:
+
+\[
+r_c \equiv R_{\rm horn}
+\]
+
+where \(R_{\rm horn}\) is the horn-torus / return-flow circulation radius.  Do
+not silently identify \(r_c\) with the local ideal-tube radius.  Instead write
+
+\[
+a_{\rm tube}=\frac{R_{\rm horn}}{\chi_h}=\frac{r_c}{\chi_h},
+\qquad
+\ell_K^{\rm phys}=2a_{\rm tube}L_{\rm cert}.
+\]
+
+Thus the dimensionless Route-B normalizer remains
+
+\[
+\mathcal N_{\rm RT}=M_{\max}L_{\rm cert}^{2},
+\]
+
+while the physical reconstruction uses
+
+\[
+L_{\rm phys}^{2}
+=
+4a_{\rm tube}^{2}L_{\rm cert}^{2}
+=
+4\frac{r_c^2}{\chi_h^2}L_{\rm cert}^{2}.
+\]
+
+Only if \(\chi_h=\chi_h(K)\) is later made topology-dependent should a separate
+horn-effective scan be introduced,
+
+\[
+\mathcal N_{\rm eff}
+=
+M_{\max}
+\left(\frac{L_{\rm cert}}{\chi_h(K)}\right)^2 .
+\]
+
+Default BEM mode remains `certified`: do not replace \(L_{\rm cert}\) by a
+horn-effective scale in BEMv1--BEMv19 results unless an explicit
+`horn-effective` scan is being run.
+
 
 BEMv8 restored the leading length scale, but the finite pair correction remained too large in raw units.
 
diff -ruN a/bem_scale_roles.py b/bem_scale_roles.py
--- a/bem_scale_roles.py	1970-01-01 00:00:00.000000000 +0000
+++ b/bem_scale_roles.py	2026-06-23 10:18:47.325738394 +0000
@@ -0,0 +1,35 @@
+"""Shared scale-role definitions for Route-B BEM.
+
+This module is informational.  It deliberately does not change numerical BEM
+results unless imported explicitly by future scans.
+
+Default convention:
+    r_c == R_horn, the horn-torus / return-flow circulation radius.
+    a_tube = r_c / chi_h, the local ideal-tube radius.
+    L_cert remains the dimensionless certified ropelength coordinate.
+"""
+
+from __future__ import annotations
+
+from dataclasses import dataclass
+
+
+@dataclass(frozen=True)
+class BEMScaleRoles:
+    """Scale-role record for physical reconstruction."""
+
+    R_horn: float
+    chi_h: float = 1.0
+
+    @property
+    def a_tube(self) -> float:
+        return self.R_horn / self.chi_h
+
+    def physical_length(self, L_cert: float) -> float:
+        return 2.0 * self.a_tube * L_cert
+
+    def certified_normalizer(self, M_max: float, L_cert: float) -> float:
+        return M_max * L_cert**2
+
+    def horn_effective_normalizer(self, M_max: float, L_cert: float) -> float:
+        return M_max * (L_cert / self.chi_h)**2
diff -ruN a/routeB_RT_bem_stecklov_falsifier.py b/routeB_RT_bem_stecklov_falsifier.py
--- a/routeB_RT_bem_stecklov_falsifier.py	2026-06-23 10:18:15.647361843 +0000
+++ b/routeB_RT_bem_stecklov_falsifier.py	2026-06-23 10:18:47.292625673 +0000
@@ -65,6 +65,36 @@
     4_1,x,y,z
 
 Author: generated for SST Route-B R--T spectral invariant research.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v10_normalizer_convergence.py b/routeB_RT_bem_v10_normalizer_convergence.py
--- a/routeB_RT_bem_v10_normalizer_convergence.py	2026-06-23 10:18:07.296758547 +0000
+++ b/routeB_RT_bem_v10_normalizer_convergence.py	2026-06-23 10:18:47.293792650 +0000
@@ -26,6 +26,36 @@
   blind_alpha_convergence_v10.md
   run_config_v10.json
   runs/<run_id>/... when BEMv9 is launched internally.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v11_length_provenance.py b/routeB_RT_bem_v11_length_provenance.py
--- a/routeB_RT_bem_v11_length_provenance.py	2026-06-23 10:18:06.636053512 +0000
+++ b/routeB_RT_bem_v11_length_provenance.py	2026-06-23 10:18:47.294995537 +0000
@@ -42,6 +42,36 @@
       Human-readable report.
 
 No observed alpha is used or compared.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v12_fourier_convention_audit.py b/routeB_RT_bem_v12_fourier_convention_audit.py
--- a/routeB_RT_bem_v12_fourier_convention_audit.py	2026-06-23 10:18:06.071739801 +0000
+++ b/routeB_RT_bem_v12_fourier_convention_audit.py	2026-06-23 10:18:47.295976072 +0000
@@ -37,6 +37,36 @@
   alpha_leading_v12_budget.csv
   fourier_convention_report.md
   run_config_v12.json
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v13_certified_length_budget.py b/routeB_RT_bem_v13_certified_length_budget.py
--- a/routeB_RT_bem_v13_certified_length_budget.py	2026-06-23 10:18:05.255934412 +0000
+++ b/routeB_RT_bem_v13_certified_length_budget.py	2026-06-23 10:18:47.297116074 +0000
@@ -39,6 +39,36 @@
 
 BEMv13 can reuse an existing BEMv8 output folder via --from-bemv8-outdir,
 or launch BEMv8 internally.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v14_certified_convergence.py b/routeB_RT_bem_v14_certified_convergence.py
--- a/routeB_RT_bem_v14_certified_convergence.py	2026-06-23 10:18:04.937071219 +0000
+++ b/routeB_RT_bem_v14_certified_convergence.py	2026-06-23 10:18:47.298443940 +0000
@@ -42,6 +42,36 @@
 
   otherwise:
       Launch BEMv13 over the requested grid.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v14_multirun_grids.py b/routeB_RT_bem_v14_multirun_grids.py
--- a/routeB_RT_bem_v14_multirun_grids.py	2026-06-23 10:18:04.280081306 +0000
+++ b/routeB_RT_bem_v14_multirun_grids.py	2026-06-23 10:18:47.299921964 +0000
@@ -1,3 +1,32 @@
+# Scale-role convention: r_c, R_horn, and a_tube
+# ----------------------------------------------
+# Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+# normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+# physical core radius into the BEM score.
+#
+# When a physical radius is discussed, use
+#
+#     r_c == R_horn
+#
+# where R_horn is the horn-torus / return-flow circulation radius.  Do not
+# silently identify r_c with the local ideal-tube radius.  Use
+#
+#     a_tube = R_horn / chi_h = r_c / chi_h
+#     ell_K_phys = 2 * a_tube * L_cert
+#
+# The dimensionless Route-B normalizer remains
+#
+#     N_RT = M_max * L_cert**2
+#
+# while physical reconstruction uses
+#
+#     L_phys**2 = 4 * a_tube**2 * L_cert**2
+#               = 4 * r_c**2 * L_cert**2 / chi_h**2
+#
+# Only if chi_h is later made knot-dependent should a separate horn-effective
+# scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+# certified dimensionless geometry mode.
+
 #!/usr/bin/env python3
 r'''
 routeB_RT_bem_v14_multirun_grids.py
diff -ruN a/routeB_RT_bem_v15_normalizer_law.py b/routeB_RT_bem_v15_normalizer_law.py
--- a/routeB_RT_bem_v15_normalizer_law.py	2026-06-23 10:18:02.169742962 +0000
+++ b/routeB_RT_bem_v15_normalizer_law.py	2026-06-23 10:18:47.301034024 +0000
@@ -17,6 +17,36 @@
     N_RT = M_max L_cert^2.
 
 No observed alpha is used or compared.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v16_heat_kernel_normalizer.py b/routeB_RT_bem_v16_heat_kernel_normalizer.py
--- a/routeB_RT_bem_v16_heat_kernel_normalizer.py	2026-06-23 10:18:01.836713485 +0000
+++ b/routeB_RT_bem_v16_heat_kernel_normalizer.py	2026-06-23 10:18:47.301981003 +0000
@@ -42,6 +42,36 @@
   bemv16_heat_kernel_appendix.tex
   bemv16_heat_kernel_report.md
   run_config_v16.json
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v17_principal_symbol_heat_kernel.py b/routeB_RT_bem_v17_principal_symbol_heat_kernel.py
--- a/routeB_RT_bem_v17_principal_symbol_heat_kernel.py	2026-06-23 10:18:00.893073235 +0000
+++ b/routeB_RT_bem_v17_principal_symbol_heat_kernel.py	2026-06-23 10:18:47.302892191 +0000
@@ -31,6 +31,36 @@
   bemv17_principal_symbol_appendix.tex
   bemv17_principal_symbol_report.md
   run_config_v17.json
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v18_multiknot_exponent_test.py b/routeB_RT_bem_v18_multiknot_exponent_test.py
--- a/routeB_RT_bem_v18_multiknot_exponent_test.py	2026-06-23 10:17:59.734408884 +0000
+++ b/routeB_RT_bem_v18_multiknot_exponent_test.py	2026-06-23 10:18:47.305784780 +0000
@@ -52,6 +52,36 @@
   bemv18_multiknot_appendix.tex
   bemv18_multiknot_report.md
   run_config_v18.json
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v3_1_sstcore_falsifier.py b/routeB_RT_bem_v3_1_sstcore_falsifier.py
--- a/routeB_RT_bem_v3_1_sstcore_falsifier.py	2026-06-23 10:18:14.579511258 +0000
+++ b/routeB_RT_bem_v3_1_sstcore_falsifier.py	2026-06-23 10:18:47.307387970 +0000
@@ -16,6 +16,36 @@
 
 It is an audit/falsifier harness, not an alpha derivation.
 No observed alpha, charge, epsilon0, electron radius, or CODATA value is used.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v3_2_idealxml_falsifier.py b/routeB_RT_bem_v3_2_idealxml_falsifier.py
--- a/routeB_RT_bem_v3_2_idealxml_falsifier.py	2026-06-23 10:18:14.356277245 +0000
+++ b/routeB_RT_bem_v3_2_idealxml_falsifier.py	2026-06-23 10:18:47.308621972 +0000
@@ -16,6 +16,36 @@
 
 It is an audit/falsifier harness, not an alpha derivation.
 No observed alpha, charge, epsilon0, electron radius, or CODATA value is used.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v3_3_idealxml_falsifier.py b/routeB_RT_bem_v3_3_idealxml_falsifier.py
--- a/routeB_RT_bem_v3_3_idealxml_falsifier.py	2026-06-23 10:18:13.548862533 +0000
+++ b/routeB_RT_bem_v3_3_idealxml_falsifier.py	2026-06-23 10:18:47.309971645 +0000
@@ -16,6 +16,36 @@
 
 It is an audit/falsifier harness, not an alpha derivation.
 No observed alpha, charge, epsilon0, electron radius, or CODATA value is used.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v3_falsifier.py b/routeB_RT_bem_v3_falsifier.py
--- a/routeB_RT_bem_v3_falsifier.py	2026-06-23 10:18:15.099676783 +0000
+++ b/routeB_RT_bem_v3_falsifier.py	2026-06-23 10:18:47.312184463 +0000
@@ -16,6 +16,36 @@
 
 It is an audit/falsifier harness, not an alpha derivation.
 No observed alpha, charge, epsilon0, electron radius, or CODATA value is used.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v4_zeta_falsifier.py b/routeB_RT_bem_v4_zeta_falsifier.py
--- a/routeB_RT_bem_v4_zeta_falsifier.py	2026-06-23 10:18:12.478562629 +0000
+++ b/routeB_RT_bem_v4_zeta_falsifier.py	2026-06-23 10:18:47.313963813 +0000
@@ -51,6 +51,36 @@
 Passing this audit does not derive alpha. A true derivation still requires a
 canonical theorem-level map alpha^{-1}=F(S_RT^ren(3_1/0_1)) specified before
 comparison with data.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v5_soft_hk_falsifier.py b/routeB_RT_bem_v5_soft_hk_falsifier.py
--- a/routeB_RT_bem_v5_soft_hk_falsifier.py	2026-06-23 10:18:11.635618655 +0000
+++ b/routeB_RT_bem_v5_soft_hk_falsifier.py	2026-06-23 10:18:47.315430253 +0000
@@ -61,6 +61,36 @@
 Passing this audit does not derive alpha. A true derivation still requires a
 canonical theorem-level map alpha^{-1}=F(S_RT^ren(3_1/0_1)) specified before
 comparison with data.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v6_convergence_grid.py b/routeB_RT_bem_v6_convergence_grid.py
--- a/routeB_RT_bem_v6_convergence_grid.py	2026-06-23 10:18:11.131124373 +0000
+++ b/routeB_RT_bem_v6_convergence_grid.py	2026-06-23 10:18:47.320446034 +0000
@@ -39,6 +39,36 @@
       Full BEMv5 output directory for each grid point.
 
 This is still a falsifier/provenance harness. It does not prove or fit alpha.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v7_spectral_length_budget.py b/routeB_RT_bem_v7_spectral_length_budget.py
--- a/routeB_RT_bem_v7_spectral_length_budget.py	2026-06-23 10:18:10.525014069 +0000
+++ b/routeB_RT_bem_v7_spectral_length_budget.py	2026-06-23 10:18:47.322042916 +0000
@@ -19,6 +19,36 @@
 
 The script can either call BEMv5 to create raw spectra, or reuse an existing
 BEMv5 output folder with --from-bemv5-outdir.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 from __future__ import annotations
 
diff -ruN a/routeB_RT_bem_v8_pair_length_budget.py b/routeB_RT_bem_v8_pair_length_budget.py
--- a/routeB_RT_bem_v8_pair_length_budget.py	2026-06-23 10:18:10.152613152 +0000
+++ b/routeB_RT_bem_v8_pair_length_budget.py	2026-06-23 10:18:47.323269401 +0000
@@ -52,6 +52,36 @@
 
 BEMv8 can run BEMv5 to generate spectra, or reuse an existing BEMv5 output
 folder via --from-bemv5-outdir.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
diff -ruN a/routeB_RT_bem_v9_correction_normalizer.py b/routeB_RT_bem_v9_correction_normalizer.py
--- a/routeB_RT_bem_v9_correction_normalizer.py	2026-06-23 10:18:08.060075550 +0000
+++ b/routeB_RT_bem_v9_correction_normalizer.py	2026-06-23 10:18:47.324684476 +0000
@@ -39,6 +39,36 @@
 -----
 BEMv9 can reuse an existing BEMv8 folder via --from-bemv8-outdir or run BEMv8
 as a substep.
+
+Scale-role convention: r_c, R_horn, and a_tube
+----------------------------------------------
+Route-B BEM is a dimensionless certified-geometry programme.  Its numerical
+normalizers use L_cert, M_max, and DeltaF_pair; they do not require inserting a
+physical core radius into the BEM score.
+
+When a physical radius is discussed, use
+
+    r_c == R_horn
+
+where R_horn is the horn-torus / return-flow circulation radius.  Do not
+silently identify r_c with the local ideal-tube radius.  Use
+
+    a_tube = R_horn / chi_h = r_c / chi_h
+    ell_K_phys = 2 * a_tube * L_cert
+
+The dimensionless Route-B normalizer remains
+
+    N_RT = M_max * L_cert**2
+
+while physical reconstruction uses
+
+    L_phys**2 = 4 * a_tube**2 * L_cert**2
+              = 4 * r_c**2 * L_cert**2 / chi_h**2
+
+Only if chi_h is later made knot-dependent should a separate horn-effective
+scan use M_max * (L_cert / chi_h(K))**2.  BEMv1--BEMv19 default mode is the
+certified dimensionless geometry mode.
+
 """
 
 from __future__ import annotations
